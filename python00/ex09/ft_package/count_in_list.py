def count_in_list(lst: list, string: str) -> int:
    """
    Counts the occurrences of the given string in a list

    Args:
    - lst (list): the list to search in
    - string (str): the string to search for

    Returns:
    - the number of occurrences of the string in the list
    """
    return lst.count(string)
